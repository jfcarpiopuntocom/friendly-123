node_modules/
*.log
.DS_Store
.env

# Backups locales con timestamp (disciplina de backup antes de editar) — se
# quedan en el disco del dev, nunca en el repo que se entrega/vende.
_local_backups/
*_20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]_[0-9][0-9]-[0-9][0-9].*
