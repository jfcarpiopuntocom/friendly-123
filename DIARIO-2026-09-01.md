# DIARIO — ventana 2026-09-01 → 2026-09-03

## Prompts de JFC (textuales)

- 2026-09-01: "hice algunos avances en la UI (la version .67 en esos files es la
  mas avanzada, pero quiero que revises las previas por si algo se perdió en el
  camino). revisa, piensa y /make-plan para integrar todo eso cuidadosamente, sin
  romper nada. a esto le vamos a llamar el 'proceso JFC': backup, debug, audit,
  check, check for languages coherence, double check the code, line count,
  checksum, audit… primero el plan, luego integras… no hagas NADA nuevo adicional…
  bajate los skills de Emil Kowalski".
- 2026-09-01: "ok, Fase A".

## Qué se hizo

1. **Plan** (`PLAN-INTEGRACION-LAB-1.7.67-2026-09-01.md`) + skills de Emil
   Kowalski vendorizados en `.claude/skills/emil-kowalski/` (12 skills oficiales,
   commit `d23d7f8`). PR #64 (borrador). JFC aprobó Fase A.
2. **Fase A — integración lab 1.7.67 → repo** (1.7.36→1.7.67, shell v149→v180).
   Detalle completo en `docs/NOTA-integracion-lab-1.7.67-2026-09-01.md`.

## Epifanías / redescubrimientos (para que otra sesión no tropiece)

- **El repo NO siempre va atrás del lab.** `auth-ui.js` iba ADELANTE en el repo
  (1644 vs 1624). Un `cp` global habría borrado el pre-chequeo anti fuerza-bruta.
  Confirma REGLA 1b: injertar, nunca sobrescribir. SIEMPRE medir por archivo.
- **`auth-ui.js` y `crypto-store.js` son un PAR.** Uno usa funciones del otro
  (`identificarPin`, `limpiarLockouts`, `fijarOwnerPin`…). No se pueden mezclar
  mitades de versiones distintas o el login se rompe. Van juntos.
- **El apodo se movió del gate al header** — parecía pérdida, era reubicación.
- **Dos decisiones fechadas se contradecían:** harness (08-28: Lord adopta) vs
  SYNCIDENTITYFIX (08-31: Lord NUNCA adopta). Ganó el más nuevo (REGLA 0); el test
  estaba rancio y se corrigió al contrato nuevo.
- **La compuerta necesita el relay + fuentes; el sandbox los bloquea.** Correr
  cada paso con el proxy desactivado (`env -u HTTPS_PROXY …`) los pone verdes. El
  harness de team-sync además chocaba con la recarga coordinada entre pestañas
  (v148) porque dos "aparatos" en un mismo navegador comparten BroadcastChannel;
  se hizo el setup resiliente (dispositivos reales no comparten canal).

## Verificación
Proceso JFC completo. Compuerta 9/9 verde (paso a paso, proxy off). Line count y
checksum casan con el lab. Backup `backup/20260901-133352` antes de tocar.

## Pendiente (Fase C — solo con OK)
i18n del apodo del header; auditoría "2+ formas"/editar datos; pulido Emil.

## Fase C (2026-09-01)
- i18n del apodo del dispositivo EN/ES (v1.7.68, PR #65 mergeado). Claves
  device.name/current/namePrompt; applyStatic sobre la vista de Avanzado.
- Pulido de animación (Emil), aprobado A/B/C/D con proceso JFC. Tras inspección:
  · A (v1.7.69): token --ease-out=cubic-bezier(.23,1,.32,1) aplicado a vistaEntra.
  · B: ya cubierto por regla global `button:active:not(:disabled)` (no se tocó).
  · C: el único 0.45s es el splash (primer arranque); Emil permite largo → no se tocó.
  · D: reduced-motion ya cubría vistaEntra y pulsoEmergencia; el splash ocSplashOut
    es red de seguridad (oculta el splash aunque el JS falle) → NO se desactiva.
  Epifanía: no meter cambios redundantes/dañinos por "completar la lista";
  ESTABILIDAD es prioridad #1. Solo A era mejora real.

## Fase C — hundimiento teclas PIN (v1.7.70, JFC lo pidió)
Candado del PIN: las teclas del keypad tenían :active de solo 1px sin sombra
(hundimiento imperceptible). Ahora canto/relieve inferior en reposo
(box-shadow:0 2px 0) que colapsa al presionar + baja 2px = se hunde. Casilla
llena con sombra interior (presionada hacia adentro). Aditivo, misma paleta
(solo sombras rgba de la tinta), reduced-motion respetado, 70ms. Verificado en
navegador (candado real, cero page-errors). auth-ui.js. Backup 20260901-141513.

## Advanced: tamaños de texto + transfers heading (v1.7.71)
- JFC: texto grotescamente grande en Advanced (todo debe ser ~tamaño de la tabla
  "Member/Role/Status" 13-14px) + rompe en horizontal. Aclaró: SOLO Advanced.
- Medido (Playwright, forzando la vista): los 7 titulos de seccion de Advanced
  eran h3.seccion a 18px (repetidos). Se caparon a 15px con CSS acotado
  `#vista-avanzado h2,h3,h4{font-size:15px !important}` — cero efecto fuera de Advanced.
- Horizontal DENTRO de Advanced: medido cero overflow (bodyScrollW==vw). El quiebre
  que JFC vio en horizontal es el header/layout GLOBAL, fuera del alcance "solo
  Advanced" — no se tocó; avisado a JFC.
- shelves.transfersHeading: nueva clave EN "Transfer your stock between shelves/racks"
  / ES "Traslada tu stock entre perchas/estantes" (antes caía al fallback).

## Commissions Opción B — idéntico a AMIGABLE (v1.7.72, idiomARTE/Belen)
Portada la FORMA de amigable sobre el backend de friendly (mismo modelo: pctMeta/hasta/comision).
- Editor de comisión con BARRA de tramos (colores) + cartel vivo modalidad artista/vendedora +
  inputs espejo asociado%↔casa% (_comisionBarraHtml, _comisionFilaHtml, abrirEditorComision).
- Gestión de comisionistas: lista viva con buscador, alta con datos de contacto/pago,
  archivar (renderGestionPromotoras/tarjetaPromotora/renderListaPromotorasViva); reemplaza
  a renderComisionistas. Overlay oc-comision-overlay. i18n EN/ES (comm.ed.*, comm.g.*, comm.ci.*).
- Arregla bug latente de friendly: su editor guardaba tramos {desde,pct} pero el backend lee
  {hasta,comision} → ahora consistente.
- friendly ya tenía cargarComisiones (tarjetas/ranking/termómetro/simulador/WhatsApp) heredado de
  amigable: NO se tocó (funciona). La matriz sigue como RFM Champions/At-risk (inglés), mismo
  concepto que Estaciones. Mobile-first (tarjetas, no tablas). Verificado en navegador (render +
  editor con barra, cero page-errors). Backup backup/20260901-*antes-commissions-opcionB.

## Team formidable — lista unificada mobile-first (v1.7.73)
JFC: una sola lista dinámica del Team, PINs visibles a owner/admin (no repetir al
crear otros), lapicitos, sincronizada con Access & recovery, calidad private panel,
y CRÍTICO mobile-first (las tablas no se leen en vertical).
- renderEmpleados (avanzado-extra.js): de <table> → TARJETAS (flex-wrap, se leen en
  vertical). Dueño encabeza; cada miembro: nombre, email, badge rol, estado, PIN
  visible (chip mono), ubicación, y acciones Deactivate/✎ PIN/rol. Bindings intactos
  (alias tbody=lista). Editor de PIN inline por tarjeta.
- Backend: GET /api/usuarios?pins=1 opt-in (el frontend solo lo pide si owner/admin);
  el PIN es local, el relay sigue zero-knowledge. Default GET sigue sin PIN.
- Sync con Access & recovery: ambos leen /api/usuarios (misma fuente).
- Verificado en navegador (390px): 2 tarjetas, sin tabla, PIN 241 visible, lapicito
  abre el editor, cero page-errors. i18n team.editPin/team.save. Backup 20260901-*team.

## Conexión FE/BE comisiones + Sold categorías con lapicito (v1.7.74)
JFC: (a) que Commissions y toda la UI estén bien conectadas (backend casa con frontend,
frameworks analíticos), (b) en Sold, lapicito a dueño/admin para agregar/editar categorías.
- AUDITORÍA: crucé todos los /api del frontend contra los handlers del backend (todos existen).
  BUG REAL cazado: /api/promotoras POST/PUT guardaban escalasComision {desde,pct} y base en
  `comision`, pero el editor portado y pctDeLaVenta/resolverTrato usan {hasta,comision} y
  comisionBase → los tramos del comisionista se perdían en silencio. Alineado el backend
  (comisionBase + {hasta,comision}, con `comision` espejo para back-compat). Round-trip
  verificado en navegador (POST/PUT/GET).
- SOLD: OCCategorias (borradores.js) ahora tiene store propio (f123_categorias_custom, local)
  + agregar()/renombrar()/listar(); renombrar propaga a productos vía PUT. En Sold, chips
  incluyen categorías propias y hay un ✎ (solo dueño/admin, gate por OCAuth.rolActual) que abre
  un gestor INLINE (no popup) para agregar o renombrar. isDueno/isAdmin NO existen en index.html
  (viven en avanzado-extra.js) → se usó OCAuth.rolActual(). i18n sold.cat* EN/ES.
- Verificado: ✎ visible (rol dueño), gestor abre, categoría nueva aparece como chip, cero
  page-errors. Backup 20260901-*antes-sold-categorias.

## SKILL.state — estado mutable de trabajo (implementación, 2026-09-01)
JFC pidió implementar arXiv:2608.26263 (SKILL.state) para tener más contexto y
reducir errores evitables siendo frugal en tokens, usando archivos estratégicos y
capturando sus instrucciones exactas.
- Creado `.claude/ESTADO.md`: estado de ejecución MUTABLE y compacto (spec inmutable
  por punteros; instrucciones duraderas de JFC verbatim; estado de subsistemas;
  trampas técnicas; última observación; protocolo). Se SOBRESCRIBE cada trabajo
  (no acumula) — a diferencia del DIARIO/bitácora que son el log append-only.
- CLAUDE.md REGLA 0-bis: leer `.claude/ESTADO.md` primero cada sesión.
- Trabajado desde el abstract (frugal, sin bajar el PDF). Solo meta-archivos; sin
  tocar código de la app. Backup 20260901-*antes-skillstate.

## Recarga de versión fuera del candado + Demo en Access & Recovery (v1.7.75)
- JFC: la recarga coordinada de versión se disparaba EN el candado del PIN y le
  reseteaba el tecleo (PC y cel). Quiere la recarga, pero diferida al paso
  siguiente (post-login), invisible, para abrir la app actualizada.
  FIX: `verificarCompatibilidadVersion` y el BroadcastChannel `f123-version`
  ahora usan `_recargarSeguroVersion()`: si `#oc-gate` visible → marca
  `f123_reload_al_entrar` y NO recarga; auth-ui.entrar() recarga solo al pasar a
  la app (ya existía ese consumidor; el 1er IIFE ya lo usaba). Verificado en
  navegador: en el gate marca el flag y navs=0 (no recarga).
- JFC: faltaba Demo en "Current access codes" (Access & Recovery). Agregada la
  línea `Demo: 456` (permanente, sin lapicito).
- NOTA/pendiente (en ESTADO.md): mejorar el inventario DEMO (cuadros, eventos,
  antigüedades — menos rock&roll); es la probada completa antes de comprar.
- Verificado: check-inline ok, guards TODO VERDE, check-sw v188. Backup del día.

## Inventario DEMO mejorado — negocio cultural creíble (v1.7.76)
JFC: mejorar SOLO el stock del DEMO (456), sin arriesgar JAMÁS los datos que los
usuarios reales ya tienen (idiomARTE sobre todo). Aclaró: demo=poblado con el
seed; fresco (789)=populan de cero ellos; real=sus buffers.
- Reescrito el seed en mock-backend.js (ubicaciones, promotoras, sucursales,
  productos, sembrarVentasDemo, gastosMensuales): Galería idiomARTE (cuadros,
  láminas, antigüedades, libros) + Consignación de artistas (modalidad artista
  85/15, María Auquilla) + Bar & Café (vinos, quesos, bebidas, comida) + Eventos
  culturales (cata de vinos y quesos, jazz, taller de acuarela, tango, foto,
  poesía). 38 productos, precios/costos/stock realistas, perecibles con caducidad,
  algún dormido, ventas sembradas coherentes con la matriz RFM.
- GARANTÍA DURA verificada en navegador: (a) demo muestra 38 prods + 4 perchas +
  57 ventas, 9 categorías, cero page-errors; (b) un usuario con buffer guardado
  carga LO SUYO (1 prod "MI PRODUCTO REAL"), NO el seed → el seed no sobrescribe
  datos de nadie. Activar 789 con vaciar arranca de cero. Datos reales intactos.
- guards TODO VERDE, check-sw v189. Backup 20260901-*antes-mejorar-demo.

---

## 2026-09-02 — Prompt multi-parte de JFC (Sold/Expenses/Cartera/Customers/Dashboard)

**Prompt (textual):** "organizate bien y no tragues tokens: termina lo de Amount
paid dentro de Sold... en vez de eso... si eligiste Counter sale... cantidad total
de ese item... / en Sold falta una lista dinamica, expandible... editarse con
lapicitos... / falta lapicito en Recent Activity pero eso pasemos al dashboard...
/ que se pueda categorizar en Expenses por tipos, best practices USA y Ecuador /
en Expenses quitar caja chica por rack/evento / renombrar Close the day a Daily
Summary / credit/abono verde y debt/fiado rojo (estan mal agrupados) / en
Sales/Sold cancelar ex post... todo a los logs con PIN y dispositivo(apodo) /
el credit (abono) solo se gasta en el mismo item / checkbox Agregar factura
si/no default no, con caja numero de factura opcional / en My customers pulldown
de rangos de edad y de pais en la misma linea / enchufa todo al dashboard.html,
dales mas control a dueño/admin, el cuaderno digital compartido / usa proceso JFC".

**Qué se hizo (v1.7.77 / shell v190), 7 lotes, todos verdes y pusheados:**
- A) Sold modal: se quitó Amount paid (#vi-monto); venta de mostrador (sin
  cliente) → campo cantidad para vender N de golpe; checkbox Add invoice (default
  off) → caja "Invoice number (optional)". Backend guarda info.factura.
- B) Sold: nuevo Sales log expandible/editable (mobile-first, tarjetas) con ✎
  (cantidad, forma de pago, factura, notas) y ✕ cancelar ex-post. Endpoints
  PATCH /api/ventas/:id y POST /api/ventas/:id/cancelar, bloqueados si la venta
  ya fue liquidada (protege plata repartida). mov() ahora graba usuarioRol +
  dispositivoApodo + dispositivoId en CADA acción (PIN jamás en claro, REGLA 8);
  movHuella intacto (no cambia el sello).
- C) Expenses: categorías por tipo (rent/utilities/inventory/payroll/services/
  marketing/transport/taxes/maintenance/other; best-practice USA+Ecuador);
  selector al registrar y editar; resumen del mes por categoría; se ELIMINÓ la
  caja chica por percha (HTML+JS). GET /api/gastos devuelve porCategoria.
- D) Cartera: credit(abono)=verde #00C87A, debt(fiado)=rojo #E8365D (semáforo);
  el saldo muestra Debt/Credit con esos colores; el crédito se registra atado a
  un item (modal con selector), guardado en motivo como [for:<item>] y mostrado
  "only for: <item>" — el crédito solo se gasta en el mismo item.
- E) My customers: pulldowns de rango de edad (18-24..65+) y país en la misma
  línea del panel editar; se muestran en la tarjeta; backend /contacto guarda
  rangoEdad+pais, fichaCliente los devuelve.
- F) Botón/FAB/modal "Close the day" → "Daily Summary" (bilingüe: Resumen diario).
- G) dashboard.html refleja todo: columnas Age/Country (Customers) y Payment/
  Invoice (Sales); el log de actividad muestra Who (usuario+rol) Y Device (apodo)
  de cada movimiento, con detalle legible.

**Epifanía / límite honesto (no alucinar):** el editar en vivo se dejó en la app
(superficie /api segura). NO se boltó edición sobre el *Activity log* sellado
(append-only anti-tamper) ni sobre el visor de sync dashboard.html (relay
zero-knowledge). Editar por esas superficies rompería salvaguardas que el propio
JFC puso (prioridades #1 ESTABILIDAD y #2 fail-safe, "nunca romper datos"). El
dashboard REFLEJA todo y el log deja constancia del responsable (usuario+rol+
dispositivo) de cada acción — que es exactamente el requisito de trazabilidad.

**Verificación:** node --check + check-inline (index/dashboard) OK; harnesses
gastos/clientes/failsafe VERDES; nuevo .claude/smoke-multiprompt.cjs VERDE
(venta cantidad 3 baja stock 3; PATCH a 2 devuelve 1; cancelar restaura al
original; categoría+porCategoria; rangoEdad/pais; log con dispositivo+rol).
Respaldo: rama backup/20260902-044526-antes-sold-expenses-dashboard-multiprompt.

---

## 2026-09-02 (2) — Crédito con expiración + cancelar reservación de evento

**Prompt (textual):** "«El credit (abono) solo se puede gastar en el mismo item.»
y debe tener fecha de expiracion (al dia siguiente de la fecha que elijan en el
iconito que expande calendario para elegir + «MM/DD/YYYY» ... expira) elegible con
esas dos maneras, para que el credito tenga un periodo o ya expire y toque dar
otro tipo de pasos / si alguien cancela un evento se le debe poder reeitar del
evento y restarle de pago/pagado, usemos worlds best practices / proceso JFC y
copiosos apuntes".

**Hecho (v1.7.78 / shell v191):**
- **Crédito con expiración (Lote H):** en el modal "Record credit (abono)" se
  agregó "Valid through" con `<input type="date">` — en en-US ofrece a la vez el
  iconito de calendario que expande Y el tecleo MM/DD/YYYY (las dos maneras en un
  control). Default = hoy. El crédito **expira al día siguiente** de la fecha
  elegida; se guarda en el motivo como `[exp:YYYY-MM-DD]` junto a `[for:item]`.
  En el saldo del cliente se muestra "expires <fecha>" y, si ya venció (hoy >=
  exp), "EXPIRED (<fecha>)" en rojo. Verificado: el motivo round-trips con
  [for:]+[exp:] en el libro mayor (AMG.Cartera).
- **Cancelar reservación de evento (Lote I):** en la lista de Guests cada reserva
  individual (owner/admin) tiene "Cancel booking": reusa `POST /api/ventas/:id/
  cancelar` que **libera el cupo** (revierte el ticket al stock) y **resta lo
  pagado** (registra montoRevertido en el log con usuario+dispositivo). Bloqueado
  si la venta ya fue liquidada a un socio. Verificado: stock restaurado y
  montoRevertido>0 tras cancelar.

**Nota de diseño (no romper después):** el crédito vive como hecho append-only
en AMG.Cartera; la expiración y el ítem se codifican en el `motivo`
(`[for:...][exp:YYYY-MM-DD]`) para NO tocar el modelo del libro mayor. La UI
(pintarSaldoCartera) parsea y marca EXPIRED; el historial solo lo ve owner/admin
(el encargado ve el saldo, no el detalle — proyección por rol de vistaCarteraSegunRol).
El "restarle de pago" del evento es la reversión de la venta (la venta cancelada
sale del total del evento), consistente con la cancelación ex-post ya existente.

**Verificación:** check-inline (index) OK, i18n EN/ES paridad OK, smoke-multiprompt
VERDE (sin regresión), nuevo `.claude/smoke-expira-evento.cjs` VERDE. Respaldo:
rama backup/20260902-050843-antes-credito-expira-y-cancelar-evento.
