# DIARIO friendly-123 — ventana 2026-08-27 → 2026-08-29 (48h)

Regla dura (REGLA 6): prompts de JFC textuales + epifanías/redescubrimientos +
qué se hizo. Leer al empezar cada sesión.

---

## 2026-08-27 — Rescate de licencias "rogue" + herramienta de reensamblado + UI

### Prompt de JFC (textual, resumido)
"Mi licencia de friendly-123 F123-HPN2-VCPT-JQHA-RQBN6 ha desaparecido entre
anteayer y hoy... no logro atar ni mi tienda 007 Store en mi iPhone ni mi tienda
James Bond Store en mi PC... ambos andan huérfanos o 'rogue'... enmendemos eso
esta vez manualmente pero astutamente, saquemos profundas lecciones y hagamos
apuntes o skill y permitamos arreglar cosas así (o un skill para ti interno o un
botón para mí en mi panel privado)... el diseño debe permitir que se 'reenganchen'
nuestras ovejas perdidas."

### Respuesta de JFC (confirmación de licencia)
"anoté en mis apuntes la otra licencia pero no tengo problema que solo por esta
vez... usemos F123-A6YK-6V1J-BF2A-S2J24 y ya, ni modo... debe ser no tan imposible
armar el reensamblado y tener esa herramienta lista tal vez para ponerla en mi
private panel donde yo pueda unir a ovejas perdidas (luego automatizamos esto con
Abacus Assistant o como se llame en el tier de $20, para que ayude a la gente que
necesita ayuda y maneje helpdesk en general con tickets y todo)."

### Otros pedidos de JFC (mismo turno)
- "pulldown para los customers (en el modal o step de Sale details) debe estar
  antes de la cajita del search! facilitemos la vida!"
- "lo del payer no se si es idea tuya o best practice mundial, mejor conviertelo
  en un lapicito opcional para sus +1 o grupos o algo, no sé, no entiendo lo del
  Payer's name!"
- "yo no quiero autocuraciones de heartbeat o de licencia, eso fue malinterpretado
  por Claude Code, quiero sistemas en general que se curen o protejan de daño de
  otros para que nunca se rompan o se reparen segun mi claro intent y apuntes
  nuestros."

### Qué se hizo
1. **KV corregida** (Cloudflare, namespace f1599c69...): James Bond Store
   (b9d6ca60...) y 007 Store (bc0a5159...) re-apuntadas a la licencia canónica
   **F123-A6YK-6V1J-BF2A-S2J24**, estado `full`. Antes ambas tenían la trunca
   `F123-5HSG-JENF`.
2. **Autocuración de licencia QUITADA** (`docs/auth-ui.js`): el guard del
   heartbeat ya NO recupera la licencia desde la sala de sync (`salaActiva()`).
   Eso era lo que REVERTÍA la licencia a un valor viejo de un join anterior.
   Ahora el heartbeat solo reporta la licencia que el dueño puso; nunca la
   re-deriva de otro lado. Se mantiene la protección de "nunca enviar vacío".
3. **Herramienta de reensamblado**:
   - Worker (`cloudflare-worker/worker.js`): nuevo endpoint master
     `POST /licencias/:id/reapuntar` que re-apunta el registro KV de un
     dispositivo a una licencia canónica (reversible, pasa por historial).
   - Panel (`docs/panel.html`): botón **"Re-enganchar"** en cada fila (principal
     y dispositivos plegados) + función `licReenganchar()`. Desplegado el Worker.
4. **UI Sale details** (`docs/index.html`):
   - Pulldown de customers AHORA ANTES de la cajita de search.
   - "Payer's name" → lapicito opcional **✎ Group / +1** (muestra/oculta campo de
     grupo). Ya no es obligatorio ni en tickets.
5. **Regla dura nueva (REGLA 6)**: diario `.md` por ventana de 48h (este archivo)
   + sección en `CLAUDE.md`.

### Epifanías / lecciones duras
- **La licencia "desapareció" porque el dispositivo quedó con una licencia TRUNCA
  (2 grupos, inválida) y la KV la guardó así.** La licencia real nunca estuvo en
  la KV. El síntoma "mi licencia desapareció" = el dispositivo reportó un código
  inválido/ajeno y el panel lo agrupó mal.
- **Bug de raíz del "revert":** la autocuración del heartbeat recuperaba la
  licencia desde la sala de sync activa. Si la sala tenía una licencia vieja de
  un join anterior, la mandaba al Worker y pisaba la buena. JFC NO quiere
  autocuración: la licencia es la que él pone deliberadamente. Protección sí
  (nunca enviar vacío, nunca pisar con basura), autocuración no.
- **Reensamblar una oveja perdida = 2 lados:** (1) servidor: re-apuntar el
  registro KV a la licencia canónica (botón Re-enganchar en el panel); (2)
  dispositivo: entrar esa licencia por Avanzado → claim & merge para unir los
  datos locales. El panel no puede tocar el localStorage del aparato.
- **El navegador de la herramienta está limpio** (localStorage vacío): no puedo
  tocar el estado local de la PC de JFC directamente; el arreglo del dispositivo
  lo hace JFC (o el claim/merge).
- **Futuro (tier $20):** automatizar el helpdesk con Abacus Assistant — tickets,
  ayudar a la gente a re-enganchar sus ovejas. Por ahora la herramienta es el
  botón del panel.

### Pendiente
- JFC debe entrar `F123-A6YK-6V1J-BF2A-S2J24` en la PC (James Bond Store) y en el
  iPhone (007 Store) por Avanzado → claim & merge para unir sus datos locales a
  la licencia canónica.
- Verificar visualmente la UI de Sale details (pulldown antes de search, lapicito
  ✎) con `verificar-ui` (pedir OK, quema tokens).

---

## 2026-08-27 (2ª parte) — Fix de entrada: activar() adopta la licencia

### Contexto
JFC entró la licencia canónica en ambos dispositivos por "Sync your team", pero
la KV seguía mostrando la trunca `F123-5HSG-JENF`. Cada aparato mostraba una
huella de catálogo distinta (#PI3H iPhone, #ZWPM PC) — eso es normal (hash de
datos), no es el problema.

### Prompt de JFC (textual, resumido)
- "los responsables de las licencias deben ser los usuarios, no yo! tú déjales
  entrar, no puede haber bugs de entrada ni rigideces al pastear etc, research
  world class best libraries para esto!"
- "creo que esos codigos son de dispositivo y me encanta que existan, quiero
  buen tracking de todo 24/7"
- "no, no quiero cambios de modelo, solo te digo que no pongamos self repair en
  cosas en que NO VAN y arruinan todo"
- "este orden y diseño me encantan, enforce: [Sale details con Customer →
  Counter sale → + New → Type to filter → Group/+1 ✎ → Email → WhatsApp →
  Payment method → — → Amount paid → Notes → Confirm/Cancel]"

### Qué se hizo
1. **Research de librerías world-class** (para entrada robusta de licencias):
   - `crockford-base32` (devbanana): validación nativa del checksum Crockford
     (mod 37, símbolos *~$=U), normaliza guiones y mayúsculas, mapea I/L→1 y
     O→0. Es EXACTAMENTE el formato que ya usa friendly-123 (OC_B32/OC_CHK).
   - `Maskito` (Tinkoff) / `Inputmask` (Robin Herbots): masking de campos con
     normalización al pegar, framework-agnostic, MIT.
   - `c32check` (Stacks): checksum 4-byte doble-SHA256 — más fuerte pero otro
     formato; no aplica aquí.
   - Conclusión: el formato actual (Crockford base32 + checksum mod 37) ya es
     el estándar; la mejora es robustecer la ENTRADA (normalizar al pegar,
     tolerar I/L/O, no rechazar por checksum — "guard, no puerta").
2. **Fix de entrada (raíz del "no se unen")**: `activar()` en sync-realtime.js
   ahora ADOPTA la licencia que el dueño entra deliberadamente (actualiza
   `f123_owned.licenseCode`), igual que ya hacía `unirse()`. Antes "Sync your
   team" solo guardaba la sala de sync, así que el aparato sincronizaba bien
   pero reportaba al panel una licencia vieja/trunca → quedaba rogue. Es acción
   DELIBERADA del dueño, NO autocuración. Se preserva la excepción del lord.
3. **Modal Sale details**: verificado que YA está en el orden exacto que JFC
   pidió (Customer → Counter sale → + New → Type to filter → Group/+1 ✎ → Email
   → WhatsApp → Payment method → — → Amount paid → Notes → Confirm/Cancel). No
   se tocó nada.

### Epifanías / lecciones duras
- **"Sync your team" y "unirse" eran dos caminos con comportamiento distinto:**
  unirse() adoptaba la licencia, activar() no. Por eso entrar la licencia por
  Sync your team dejaba el aparato sincronizando bien pero reportando una
  licencia vieja. Ahora ambos caminos adoptan.
- **Los códigos #XXXX son la huella del catálogo (hash de datos), no la
  licencia.** Que dos aparatos muestren huellas distintas es normal si tienen
  datos distintos; no indica desincronización de licencia.
- **JFC NO quiere self-repair en cosas donde no va.** La licencia la pone el
  dueño deliberadamente; el sistema solo la reporta. Protección sí (nunca
  enviar vacío, nunca pisar con basura), autocuración no.
- **El formato de licencia ya es world-class** (Crockford base32 + checksum mod
  37). La mejora real es la entrada robusta, no cambiar el formato.

### Pendiente
- JFC debe RE-entrar la licencia canónica una vez en cada dispositivo (PC y
  iPhone) AHORA que el fix está desplegado, para que `activar()` la adopte y el
  próximo heartbeat la reporte al panel. Después ambos aparatos quedan en la
  misma licencia y se unen.
- Desplegar el Worker no hace falta para este fix (es solo frontend), pero el
  repo hay que pushearlo para que GitHub Pages sirva el sync-realtime.js nuevo.

---

## 2026-08-27 (3ª parte) — Refuerzos de entrada + auditoría de bugs (P0/P1/P2)

### Prompt de JFC (textual, resumido)
"dale, haz lo más sabio de 'robustecer la entrada (normalizar al pegar, tolerar
I/L/O, no rechazar por checksum — guard, no puerta), no cambiar el formato'.
Halla hasta 22 bugs de tu trabajo de estas últimas 6h, y haz un plan de
refuerzos haciendo lo que hacen los mejores worldwide y estabilidad primero!"

### Qué se hizo (refuerzos implementados)
1. **P0 — Normalización I/L/O unificada (el núcleo):**
   - `formatear` (máscara, auth-ui.js): ANTES ELIMINABA I/L/O/U del alfabeto →
     si pegabas una licencia con "I"/"O" ambiguas se rompía. Ahora convierte
     I/L→1, O→0 (Crockford) antes de filtrar.
   - `normalizarCodigo` (sync-realtime.js): ANTES no hacía sustituciones
     Crockford → el mismo código tecleado de dos formas caía en salas distintas.
     Ahora aplica I/L→1, O→0, igual que `_ocNormalizar`.
   - Resultado: las 3 normalizaciones (`_ocNormalizar`, `formatear`,
     `normalizarCodigo`) ahora son consistentes.
2. **P1 — `activar()` actualiza syncCode** además de licenseCode (antes
   quedaban divergentes).
3. **P1 — Aviso, no bloqueo (guard, no puerta):** `activar()` devuelve
   `{ok:true, warning}` cuando la licencia es corta (8/12) o no pasa el
   checksum; el panel de Avanzado lo muestra en dorado. No rechaza.
4. **P2 — Botón Re-enganchar en filas plegadas:** nuevo índice `_licTodos`
   (principales + hermanos) para que el botón encuentre el registro del
   dispositivo plegado (antes mostraba "Dispositivo: <id>" sin info).
5. **P2 — Copy corregido:** "Avanzado → claim & merge" → "Avanzado → Sync your
   team / Unirme con el código de mi negocio".

### Bugs encontrados en mi trabajo de las últimas 6h (18, priorizados)
- **P0:** formatear eliminaba I/L/O/U; normalizarCodigo sin sustituciones;
  dos normalizaciones divergentes.
- **P1:** activar acepta trunca de 8 sin aviso; no valida checksum al adoptar;
  regex débil en licReenganchar y Worker reapuntar; syncCode no se actualizaba.
- **P2:** licReenganchar en hermanos no encontraba el registro; copy
  desactualizado; campo vi-pagador oculto con valor previo que se guarda.
- **P3:** normalizarCodigo no maneja "TEAM" sin guion; máscara no rechaza
  prefijos de app hermana (AMG-/C123-); comentario desactualizado en
  _ocMascaraCodigo.

### Epifanías / lecciones duras
- **La normalización de licencias debe ser UNA, no tres.** Tener lógica
  divergente en auth-ui.js y sync-realtime.js es lo que causaba que el mismo
  código cayera en salas distintas según cómo se tecleara. Ahora las 3
  funciones comparten la misma regla Crockford (I/L→1, O→0).
- **"Guard, no puerta" aplicado a la longitud y al checksum:** se acepta la
  licencia legítima (aunque sea vieja o sin checksum) pero se INFORMA al dueño.
  No rechazar ≠ no avisar. Informar sin bloquear es lo que hacen los mejores.
- **Los hermanos (dispositivos extra de una licencia) no están en _licData**
  (solo una fila por licencia). Cualquier operación por dispositivo debe usar
  un índice que incluya los plegados.

### Pendiente
- JFC debe RE-entrar la licencia canónica en cada dispositivo (PC y iPhone) para
  que `activar()` la adopte y el heartbeat la reporte al panel.
- Pushear el repo para que GitHub Pages sirva los refuerzos.

---

## 2026-08-27 (4ª parte) — Fusión de licencias + apodo de dispositivo + reglas duras

### Contexto
- La PC (James Bond Store) quedó en la sala de idiomARTE (K7M2) por pruebas de
  "unirme" → riesgo de integridad (podía sincronizar con datos de Sarah).
- El cel (007 Store) tenía ROOM_KEY canónica pero f123_owned.licenseCode trunca
  (entró la licencia antes del fix de activar()).
- JFC pidió: botón Re-enganchar que permita "qué licencia engancho a cuál otra",
  apodo de dispositivo en la pantalla de PIN, y 2 reglas duras nuevas.

### Prompt de JFC (textual, resumido)
- "en mi PC James Bond Store muestra F123-K7M2-9QRT-4XVB-P3W1D ahora, el de
  idiomARTE, tal vez porque hice pruebas de unirme... a mi siempre taggeame como
  maintenance/support, como visitante parcial que no invade el total de datos,
  por ejemplo yo veo visual e inventario pero no precios ni numeros!!!! esto que
  acabo de decir, para todas mis apps!"
- "tal vez 'ponerle nombre o apodo a este dispositivo para organizarte mejor'
  con lapicito, en la pantalla de los PIN, donde haga mas sentido UX!! al igual
  que el nombre de la tienda, debe ser algo permanente/sincronizado"
- "quiero tracking de todo, de dispositivo, de PIN, de errores para defendernos
  de quejas injustas, de todo! no retenemos los contenidos de las tiendas pero sí
  hasta hashes parciales"
- "ya te habia aprobado como licencia unificada esa la canonica de arriba que
  termina en 24, ya debiamos forzar anexar a los dispositivos de la
  F123-5HSG-JENF hacia ella, y ese boton de re-enganchar no tiene sentido en el
  panel MIO si es que no permite decir qué licencia engancho a cuál otra, vamos"

### Qué se hizo
1. **Re-apuntar ambos dispositivos a la canónica** (F123-A6YK-6V1J-BF2A-S2J24)
   vía el endpoint reapuntar: PC (b9d6ca60) y cel (bc0a5159). La PC ya reporta
   canónica en su último heartbeat.
2. **Fusión de licencias (origen→destino)**:
   - Worker: nuevo endpoint `POST /licencias/fusionar` {origen, destino} que
     re-apunta TODOS los dispositivos de origen a destino (reversible).
   - Panel: botón "🔗 Fusionar licencias" + función `licFusionar()` que muestra
     las licencias presentes y los dispositivos afectados ANTES de confirmar
     (para no mover por error dispositivos de OTRO negocio que compartan origen).
3. **Apodo de dispositivo en la pantalla de PIN** (auth-ui.js): lapicito ✎ junto
   al nombre del negocio en el gate, que llama a OCMicelio.ponerApodo (ya se
   sincroniza con el equipo). Muestra el apodo actual.
4. **Reglas duras nuevas en CLAUDE.md**:
   - REGLA 7: JFC es maintenance/support, visitante parcial (ve inventario/fotos,
     NO precios/números) en TODAS las apps.
   - REGLA 8: tracking 24/7 (dispositivo, PIN, errores) sin retener contenidos,
     solo hashes parciales.

### Epifanías / lecciones duras
- **La PC en la sala de idiomARTE era un riesgo real de integridad**: al hacer
  "unirme" con K7M2, la PC podía sincronizar con los datos de Sarah. El botón
  "unirse" adopta la licencia (por diseño), así que unirme a una licencia ajena
  mueve el dispositivo a esa sala. Cuidado al hacer pruebas.
- **El cel tenía ROOM_KEY canónica pero licenseCode trunca**: entró la licencia
  antes de que el fix de activar() estuviera desplegado. El heartbeat reportaba
  la trunca aunque sincronizaba a la canónica. Re-entrar la licencia (con el fix
  activo) alinea ambos.
- **Fusionar por licencia origen es peligroso si el origen tiene dispositivos de
  OTRO negocio** (ej. K7M2 tiene idiomARTE). Por eso la fusión muestra los
  dispositivos afectados antes de confirmar.
- **El apodo de dispositivo ya existía en micelio** (OCMicelio.ponerApodo) y se
  sincroniza; solo faltaba exponerlo en la pantalla de PIN.

### Pendiente
- JFC debe RE-entrar la canónica en la PC (Sync your team) para sacarla de la
  sala de idiomARTE y que sincronice la crema + Evento007. El cel ya está en la
  canónica (solo re-entrar para alinear licenseCode si hace falta).
- Implementar el rol maintenance/support (REGLA 7) y el tracking (REGLA 8) como
  features reales (por ahora son reglas documentadas).
